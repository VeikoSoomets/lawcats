cron:

- description: craws laws from riigiteataja (we don't want to do 350 http calls on each search
  url: '/sys/download_riigihtml'
  schedule: every 24 hours

#- description: crawls valid user requests, saves results and maps with user requests every hour
#  url: '/sys/datastore_req'
#  schedule: every 60 mins
#
#- description: automatically archives old results every day
#  url: '/sys/auto_archive'
#  schedule: every 24 hours
